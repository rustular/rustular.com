---
import { Image } from 'astro:assets';

type Props = {
  name: string;
  profilePic: string;
  testimonial: string;
}

const { name, profilePic, testimonial } = Astro.props;
---

<div
  class="flex w-72 flex-col items-center rounded-md border border-slate-400 bg-slate-900 p-2"
>
  <div class="flex">
    <Image
      class="rounded-full"
      src={profilePic}
      alt={`${name}'s profile picture'`}
      width={48}
      height={48}
    />
    <div class="my-auto ml-2 text-xl font-bold">{name}</div>
  </div>
  <div class="pt-2 font-light">{testimonial}</div>
</div>
